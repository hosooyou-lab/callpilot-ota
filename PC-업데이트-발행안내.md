# 콜파일럿 PC 자동 업데이트 — 발행 안내

코드를 고친 뒤 **마스터콘솔에서 버튼 한 번**이면 전 PC가 자동으로 새 버전을 받습니다.
(더 이상 zip 만들어서 사람마다 전달할 필요 없음. zip은 "처음 설치할 때"만 씁니다.)

## 평소 발행 절차 (코드 고친 뒤)

1. `docs\admin.html`(마스터콘솔)을 브라우저로 연다.
2. **[① src 폴더 선택]** → `C:\Users\uchbu\Dev\callpilot-pc\resources\app\src` 선택.
   → 바뀐 파일 목록이 자동으로 나온다.
3. 변경 내용 메모 한 줄 쓰고 **[② 업데이트 발행]** 클릭. 끝.

각 상담사 PC는 **앱을 켤 때(켜져 있으면 3시간 안에)** 자동으로 받아서
"업데이트 완료 — 지금 다시 시작할까요?" 안내가 뜬다. 상담사는 버튼만 누르면 된다.
앱 메뉴 **도움말 → 🔄 업데이트 확인**으로 즉시 받게 할 수도 있다.

## 첫 1회 설정 (콘솔 하단 [⚙ 발행 설정])

- **GitHub 토큰**: github.com → Settings → Developer settings → Fine-grained tokens →
  Repository access = `callpilot-ota`만, Permissions = **Contents: Read and write**만 → 생성 → 붙여넣기.
- **서명키**: `Downloads\콜파일럿_업데이트서명키(비공개-배포금지).json` 내용 전체를 붙여넣기.
  이 키로 서명된 업데이트만 앱이 믿는다. **절대 남에게 주거나 저장소에 올리지 말 것.**

## 문제가 생겼을 때

- **잘못 발행함** → 이 PC의 src를 고치고(또는 이전 코드로 되돌리고) 다시 발행하면 된다.
  각 PC는 "버전"이 아니라 "파일 내용"을 비교하므로 그대로 예전 상태로 돌아간다.
- **한 PC만 이상함** → 그 PC의 `%APPDATA%\CallScriptMemo\update-backup\`에 직전 파일 백업이 있고,
  `update.log`에 적용 기록이 남는다.
- **업데이트가 안 감** → 발행은 됐는지 콘솔 상단 "현재 발행" 확인 → 그 PC 인터넷 확인 →
  앱 도움말 → 업데이트 확인을 눌러보면 실패 사유가 뜬다.

## 한계 (이건 여전히 zip 재배포)

- `콜파일럿.exe`·`node_modules`·`adb` 변경(일렉트론 업그레이드 등)은 자동 업데이트 대상이 아니다.
  이 경우에만 예전처럼 zip을 새로 만들어 전달한다 (license.dat·*.bak 제외 규칙 동일).
- 새 PC 첫 설치도 zip으로 (설치 후부터는 자동).

## 참고

- 발행 = 이 저장소의 `pc/src/`에 파일 커밋 + `pc/manifest.json`(서명 포함) 갱신.
  기록은 `pc/history/`에 버전별로 남는다.
- 이 저장소는 공개(public)라서 발행된 소스가 보인다(모바일 dist.zip과 같은 노선).
  개인정보·비밀키는 코드에 없고, 앱 사용은 로그인·기기인증으로 잠겨 있다.
- **콘솔로 발행한 뒤 이 폴더에서 git 작업(모바일 OTA 등)을 할 땐 먼저 `git pull`** —
  발행이 GitHub에 직접 커밋되므로 로컬이 한 발 늦은 상태가 된다.
